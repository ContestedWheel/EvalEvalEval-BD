# Use this one if you want to send a message in the same server
await ctx.send("text")

# Use this one if you want to send a message in a different channel.
channel = bot.get_channel(channel id here)
await channel.send('text') 
